import Button from "../ui/button";

const LandCta = ({}) => {
  return (
    <Button className="bg-white text-slate-900 hover:bg-white hover:text-primary hover:scale-105 transition-transform ease-out duration-300">
      Book An Appointment
    </Button>
  );
};

export default LandCta;
