import { Heading } from "@/components";
import { FC } from "react";

interface supportProps {}

const Support: FC<supportProps> = ({}) => {
  return <Heading>Support</Heading>;
};

export default Support;
